# Copy to .env and fill in your values. Do not commit .env.
# App (Expo) loads this from project root. Functions load it when running locally (emulator).
# For production deploy, set RAZORPAY_KEY_SECRET and ADMIN_PASSWORD via Firebase config or Cloud env.

# -----------------------------------------------------------------------------
# Firebase (client)
# -----------------------------------------------------------------------------
EXPO_PUBLIC_FIREBASE_API_KEY=
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=
EXPO_PUBLIC_FIREBASE_PROJECT_ID=
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
EXPO_PUBLIC_FIREBASE_APP_ID=

# -----------------------------------------------------------------------------
# App – Agora & Razorpay (client)
# -----------------------------------------------------------------------------
EXPO_PUBLIC_AGORA_APP_ID=
EXPO_PUBLIC_RAZORPAY_KEY_ID=

# -----------------------------------------------------------------------------
# Cloud Functions (Razorpay + admin). Used when running emulator from root.
# In production set via: firebase functions:config:set razorpay.key_id="..." etc.
# -----------------------------------------------------------------------------
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
ADMIN_PASSWORD=
